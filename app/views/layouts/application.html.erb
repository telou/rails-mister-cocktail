<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
    <title>RailsMisterCocktail</title>
     <%= csrf_meta_tags %>
    <%= action_cable_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all' %>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    <link href="https://fonts.googleapis.com/css?family=Kaushan+Script|Open+Sans+Condensed:300&display=swap" rel="stylesheet">
  </head>

  <body>

    <% if params[:action] != 'home' %>
      <div class="title-row">
        <%= link_to root_path do %>
          <p class="title">The Cocktail Club</p>
        <% end %>
          <div class="small-box pl-5">
            <%= form_tag(cocktails_path, method: :get) do %>
              <div class="search-box">
                <%= text_field_tag(:search, params[:search], placeholder: "Search cocktails") %>
                <%= submit_tag ("Search"), class:"search-button col-4" %>
              </div>
            <% end  %>
            <%= link_to cocktails_path do %>
              <p class="view-all">View all</p>
            <% end %>
          </div>
      </div>
    <% end %>

    <%= yield %>
      <!-- from app/javascript/packs/application.js -->

  </body>
</html>
